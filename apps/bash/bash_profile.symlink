# Please note, .bash_profile is executed for login shells, while
# .bashrc is executed for interactive non-login shells.
#
# Exception Terminal.app on OS X: for each new terminal window,
# .bash_profile is called instead of .bashrc. 

[[ -s ~/.bashrc ]] && source ~/.bashrc
